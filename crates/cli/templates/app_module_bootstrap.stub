use elif::prelude::*;
use crate::controllers::{{ controller_name }};
use crate::services::{{ service_name }};

#[module(
    controllers: [{{ controller_name }}],
    providers: [{{ service_name }}],
    is_app
)]
pub struct AppModule;