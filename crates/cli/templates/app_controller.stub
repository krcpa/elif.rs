use elif::prelude::*;
use crate::services::AppService;

#[controller("/api")]
pub struct AppController {
    app_service: AppService,
}

impl AppController {
    #[get("/health")]
    pub async fn health(&self) -> HttpResult<ElifResponse> {
        Ok(ElifResponse::ok().json(&self.app_service.get_health())?)
    }
}