[package]
name = "elif-orm"
version = "0.1.0"
edition = "2021"
description = "ORM and database layer for the elif.rs web framework (Phase 2 development)"
license = "MIT"
authors = ["krcpa <krcpa@users.noreply.github.com>"]
repository = "https://github.com/krcpa/elif.rs"
homepage = "https://github.com/krcpa/elif.rs"
documentation = "https://docs.rs/elif-orm"
keywords = ["orm", "database", "web", "framework", "sql"]
categories = ["web-programming", "database"]

[dependencies]
elif-core = { version = "0.1.0", path = "../core" }
sqlx = { workspace = true }
serde = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }