[package]
name = "elif-openapi-derive"
version = "0.1.0"
edition = "2021"
description = "Derive macros for elif-openapi"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
serde_json = "1.0"