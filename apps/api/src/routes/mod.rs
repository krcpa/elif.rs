use elif_http::ElifRouter;

pub fn router() -> ElifRouter {
    ElifRouter::new()
    // API routes will be added here by the framework's code generation
    // This serves as the base router for application-specific routes
}